--- a/xenserver/etc_init.d_vswitch	Thu Oct 01 17:26:46 2009 +0100
+++ b/xenserver/etc_init.d_vswitch	Fri Oct 02 11:35:38 2009 +0100
@@ -265,11 +265,11 @@
         warning "$VSWITCHD_CONF does not exist"
         action "Creating empty $VSWITCHD_CONF" touch "$VSWITCHD_CONF"
     else
-	/usr/bin/ovs-cfg-mod '-vANY:console:emer' -F "$VSWITCHD_CONF" \
-	    '--del-match=bridge.*' \
-	    '--del-match=port.*' \
-	    '--del-match=bonding.*' \
-	    '--del-match=iface.*'
+        /usr/bin/ovs-cfg-mod '-vANY:console:emer' -F "$VSWITCHD_CONF" \
+            '--del-match=bridge.*' \
+            '--del-match=port.*' \
+            '--del-match=bonding.*' \
+            '--del-match=iface.*'
     fi
 
     start_vswitchd
